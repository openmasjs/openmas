export function buildBootResult({
  bootId,
  status,
  requestedBy,
  strict,
  projectRootPath,
  masRootPath,
  masRootHint,
  projectValidation,
  masValidation,
  warnings,
  errors,
  nextStep,
  invocationReadiness,
  startedAt,
  finishedAt,
  persistence,
}) {
  return {
    bootId,
    status,
    requestedBy,
    strict,
    projectRootPath,
    masRootPath,
    masRootHint,
    projectValidation,
    masValidation,
    warnings,
    errors,
    nextStep,
    invocationReadiness,
    startedAt,
    finishedAt,
    persistence,
  };
}
